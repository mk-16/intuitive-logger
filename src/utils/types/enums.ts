export enum RegularLogKind {
    Function = "function",
    Object = "object",
}
export enum DecoratorLogKind {
    Method = "method",
    Property = "property",
    Constructor = "constructor"
}

export enum LogType {
    decorator = "decorator",
    regular = "regular"
}