import { describe } from "mocha";
import { expect } from "expect";
import { Logger } from "./logger.js";


describe("Logger", () => {
    it("tracks", () => {    })
    it("decorates", () => {    })
});