import { TrackOptions } from "../../../utils/types/types.js";

export abstract class LegacyDecoratorTracker {
    public static track(target: any, propertyKey?: string, descriptor?: PropertyDescriptor, options?: TrackOptions) {

    }
}
